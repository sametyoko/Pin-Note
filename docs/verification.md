# 確認結果

## 2026-09-18：ソース整形・VS Code設定

- Java・XMLは4スペース、TypeScript・TSX・JavaScript・CSS・HTML・JSON・Markdownは2スペースを基準に整形。1行に詰まっていたソースも改行して展開。
- `.editorconfig`、Prettier設定、VS Codeの保存時整形設定と推奨拡張を追加。`frontend`で`npm run format`／`npm run format:check`を実行可能。
- 依存ライブラリ、生成物、生成ツールは対象外。適用済みFlyway移行SQL4件はSHA-256で変更がないことを確認。
- 整形チェック、フロントの回帰テスト5件、TypeScript / Viteビルドが成功。
- 整形後のDB結合テスト8件とMaven packageも成功。アプリを再起動し、HTML・JS・CSSが最新版と一致してHTTP 200で配信されることを確認。

## 2026-09-18：Smoke Glass・検索・お気に入り

- 案④を基に白／灰色の半透明パネルとオレンジへ変更。ホバー・押下は濃いオレンジ。取り消し／やり直しはヘッダーの下の右側に独立配置。
- マイポイントで名前・メモ・市区町村を検索。全角半角・英字大小文字を正規化し、空白区切りはAND検索。検索は現在のユーザーが取得済みの記録だけを対象とし、順序は変更しない。
- 一覧と詳細の星でお気に入りをDB保存。お気に入りだけの変更は一覧の順序を変えない。所有者チェック、取り消し／やり直し、編集・削除復元後の維持を検証。
- 既存履歴のスナップショットにfavorite=falseを補完するV4移行を追加し、古い記録の復元との互換性を維持。
- ピンを選ぶと輪と大きさで強調し、下から詳細カードを表示。地図の中心を補正して選択ピンをカードの上に表示。写真は4:3・角丸に統一。動きを減らす端末設定ではアニメーションを無効化。
- バックエンド結合テスト8件、フロントの検索・地図テスト5件が成功。TypeScript / Viteビルド成功。
- 今回はブラウザー接続が成功。390×844と320×740でヘッダー下の履歴ボタン、リスト／格子表示、詳細カードと選択ピンを目視確認。メモ検索で3件から1件に絞り込み、お気に入り保存、詳細から編集して写真を保持したまま地図に戻る動作も確認。

## 2026-09-17：紹介文の非表示・やり直し

- 地図の紹介文に44pxの×ボタンを追加。ブラウザーに非表示設定を保存し、設定画面から再表示できる。
- 右向き矢印で取り消した登録・編集・削除をやり直せる。写真も対象。既存の履歴テーブルに区分を追加し、取り消し・やり直しの合計で10操作分を永続保存。新しい変更でやり直し履歴を破棄。
- PostgreSQL結合テスト7件成功。写真付きの登録→編集→削除を取り消してやり直す順序、二重操作の拒否、他ユーザーからのアクセス拒否、新規変更によるやり直しの破棄、10操作の往復を検証。
- フロントの地図回帰テスト2件とTypeScript / Viteビルド成功。
- ブラウザー接続初期化エラーが継続しているため、実画面のクリック・スマホ表示の目視確認は未実施。

## 2026-09-16：ポイント保存後の白画面を修正

- 登録画面で地図が非表示になるとLeafletがサイズ0を保持し、保存後の `flyTo` で `Invalid LatLng object: (NaN, NaN)` が発生することを再現。
- 地図の表示状態を渡し、非表示中は移動を停止。表示に戻るとサイズを再計算してから移動し、ResizeObserverも非表示のサイズ0を反映しないように修正。
- 実際のReact・Leafletを使用したJSDOM回帰テスト2件を追加。修正前は両方で座標エラー、修正後は両方成功。保存後のピン表示と、非表示中に位置が変更された場合の復帰を確認。
- TypeScript / Viteビルド成功。起動中の `/points/` と参照するJS・CSSがHTTP 200で、修正後のビルドと内容が一致することを確認。
- ブラウザー自動操作は接続初期化エラーにより利用できず、実ブラウザー上でのクリック確認は未実施。

フロントの回帰テストは `cd frontend` のあと `npm test` で実行できる。

## 2026-09-15：ログイン・記録保護

- ユーザー名とパスワードによる登録・ログイン・ログアウトを実装。パスワードはBCryptでハッシュ化。
- ポイント・写真・操作履歴をログインユーザーごとに分離。
- 登録・編集・削除の直近10操作を取り消せる。写真も復元対象。やり直し（redo）は対象外。
- PostgreSQLを使う結合テスト5件成功、失敗・スキップ0件。登録・画像検証に加え、他ユーザーの記録へのアクセス拒否、ログイン／ログアウト、写真付き復元、履歴10件制限、古い履歴への二重取り消し拒否を確認。
- 実HTTPでも未ログイン時の401、アカウント登録、記録作成・削除、ログアウト後の拒否を確認。
- DBとアプリを停止・再起動し、同じアカウントで再ログインして保存済み履歴から削除済みの記録を復元できた。検証用のアカウント・記録・履歴は削除済み。
- React / TypeScript / Viteの本番ビルド成功。JSは約771KB（gzip約241KB）でチャンクサイズ警告あり。
- フロントを含む最新版の実行可能JARのpackage成功（結合テスト5件は別途実行済み）。
- ログイン導入前の所有者未設定の記録1件は削除せず保持。所有者が決まるまでは各アカウントの画面には表示しない。
- 今回の認証画面と取り消しボタンのブラウザー操作確認は、ブラウザーツール初期化エラーにより未実施。下記の過去の画面確認とは区別する。

アプリ再起動後は再ログインが必要。パスワード再設定は未実装。公開運用時のHTTPS設定や運用対策は今回のローカル動作確認には含めていない。

## 初期版の確認結果（過去の記録）

2026-09-12

## 確認済み

- React / TypeScript / Viteの本番ビルド成功。
- Java 17向けコンパイル成功（このMacのJDK 25で検証）。
- PostgreSQL 14.18を使った結合テスト2件成功、失敗・スキップ0件。
  - 自動名で登録、写真保存と配信、同一リクエスト再送時の重複防止、写真保持編集、削除。
  - 緯度の範囲外、画像を偽装したファイル、必要ヘッダーのない更新リクエストを拒否。
- 9月11日に登録した検証用ポイントとPNGが、DB通常停止とアプリ停止後、9月12日の再起動で取得できた。
- 保存済み写真のHTTP 200・image/png・画像データを確認。検証用ポイントは内容が一致することを確認して削除済み。
- `/points/` のHTMLと参照するJS・CSS・faviconすべてHTTP 200。
- `/`、`/enshu/index`、`/kadai/index` はHTTP 200。
- 東京駅の公開座標から市区町村「千代田区」を取得。
- Maven package成功。フロントを含む実行可能JARは target/demo-0.0.1-SNAPSHOT.jar。

## 画面確認の範囲

前回作業で390×844のスマホ表示を確認し、実地図・ヘッダー・下部ナビ・地点選択・登録フォームをブラウザーで確認した。スキップ保存ボタンを登録画面上部へ移動する修正後もフロントのビルドは成功。

再開後はブラウザー自動操作ツールの接続エラーで、最後のクリック操作の再確認は実施できていない。APIと配信ファイルは上記のとおり確認済み。実端末でのGPS許可・拒否、写真の向き、320px幅、リスト／格子切替の最終実機確認は未実施。

## 利用前に把握すること

- 初期版では認証未実装。2026-09-15に上記の最小認証機能を追加。
- 市名取得・地図には外部通信が必要。取得不可時も日時のみで登録できる。
- 現在の日時は登録時の端末時刻、DBの作成日時はサーバー側で記録する。
- 写真はJPEG・PNG対応。iPhoneのHEIC・WebPには未対応。
- フロントのビルドには約766KB（gzip約240KB）のJSチャンク警告があるが、ビルドエラーではない。必要に応じて今後分割する。
