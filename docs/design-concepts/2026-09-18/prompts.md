# Pin Note design concepts

Generated with the built-in image generation tool. Design exploration only.

## 01 Frost Glass

Use case: ui-mockup. Generate ONE polished design concept image for the existing Japanese mobile location bookmarking web app "pin note". Concept 01 Frost Glass. Floating frosted white glass panels with strong but tasteful backdrop blur over pale grayscale street map. Rounded 24px corners, fine white rims, airy premium feel. Main map view, ample visible map, orange saved pins, small dismissible intro chip with X, compact selected place bottom sheet.
Composition: one large straight-on mobile app viewport, 390x844 proportions, centered on a pale gray presentation canvas, no angled phone or hands, no device hardware. Small concept title above, a compact two-button interaction swatch below the viewport labeled "通常" and "ホバー", showing normal orange #E87524 and darker burnt orange #B84A12. Both buttons have white text "+ 場所を追加". Make the UI readable and plausible, not a marketing poster. White #FAFAFA and gray #E5E7EB dominate, warm orange is the sole strong accent. Translucency should visibly reveal subtle map lines through panels; avoid excessive gloss, rainbow, blue, purple or illegible pale text.
Viewport content: minimal header with "pin note", Material Design-style undo left arrow, redo right arrow, account outline icon, all spaced to fit smartphone. Grayscale Japanese urban map with several orange map pins, small location button. Compact bottom call-to-action "この場所を追加". Bottom navigation two items "地図" and "ポイント", active map highlighted orange. No oversized hero slogan, no login form, no extra features. Japanese sans-serif typography. Maintain 44px touch targets. Show a tasteful semi-transparent surface treatment consistent across components. This is design exploration, not a screenshot of a running app.

## 02 Clear Minimal

Use case: ui-mockup. Generate ONE polished design concept image for the existing Japanese mobile location bookmarking web app "pin note". Concept 02 Clear Minimal. Extremely restrained transparent clean UI, almost flat, thin gray hairline borders, 12px corner radius, very light shadows, translucent white compact top toolbar and low bottom action bar. Maximum map visibility, grayscale map orange pins. Editorial whitespace and simple functional geometry.
Composition: one large straight-on mobile app viewport, 390x844 proportions, centered on a pale gray presentation canvas, no angled phone or hands, no device hardware. Small concept title above, a compact two-button interaction swatch below the viewport labeled "通常" and "ホバー", showing normal orange #E87524 and darker burnt orange #B84A12. Both buttons have white text "+ 場所を追加". Make the UI readable and plausible, not a marketing poster. White #FAFAFA and gray #E5E7EB dominate, warm orange is the sole strong accent. Translucency should visibly reveal subtle map lines through panels; avoid excessive gloss, rainbow, blue, purple or illegible pale text.
Viewport content: minimal header with "pin note", Material Design-style undo left arrow, redo right arrow, account outline icon, all spaced to fit smartphone. Grayscale Japanese urban map with several orange map pins, small location button. Compact bottom call-to-action "この場所を追加". Bottom navigation two items "地図" and "ポイント", active map highlighted orange. No oversized hero slogan, no login form, no extra features. Japanese sans-serif typography. Maintain 44px touch targets. Show a tasteful semi-transparent surface treatment consistent across components. This is design exploration, not a screenshot of a running app.

## 03 Soft Pearl

Use case: ui-mockup. Generate ONE polished design concept image for the existing Japanese mobile location bookmarking web app "pin note". Concept 03 Soft Pearl. Soft milky translucent pearl-white layered cards, gentle diffuse shadows, warm light gray base, rounded 30px corners and friendly pill buttons. Map background faintly visible through surfaces. Small dismissible intro with X. A selected place photo thumbnail of a Japanese street cafe within compact translucent bottom card.
Composition: one large straight-on mobile app viewport, 390x844 proportions, centered on a pale gray presentation canvas, no angled phone or hands, no device hardware. Small concept title above, a compact two-button interaction swatch below the viewport labeled "通常" and "ホバー", showing normal orange #E87524 and darker burnt orange #B84A12. Both buttons have white text "+ 場所を追加". Make the UI readable and plausible, not a marketing poster. White #FAFAFA and gray #E5E7EB dominate, warm orange is the sole strong accent. Translucency should visibly reveal subtle map lines through panels; avoid excessive gloss, rainbow, blue, purple or illegible pale text.
Viewport content: minimal header with "pin note", Material Design-style undo left arrow, redo right arrow, account outline icon, all spaced to fit smartphone. Grayscale Japanese urban map with several orange map pins, small location button. Compact bottom call-to-action "この場所を追加". Bottom navigation two items "地図" and "ポイント", active map highlighted orange. No oversized hero slogan, no login form, no extra features. Japanese sans-serif typography. Maintain 44px touch targets. Show a tasteful semi-transparent surface treatment consistent across components. This is design exploration, not a screenshot of a running app.

## 04 Smoke Glass

Use case: ui-mockup. Generate ONE polished design concept image for the existing Japanese mobile location bookmarking web app "pin note". Concept 04 Smoke Glass. Sophisticated smoky light-gray translucent panels over a detailed but subdued white-gray street map, medium rounded 18px corners, stronger charcoal typography, sharp orange pins, restrained architectural geometric layout. Clearly translucent, light theme not dark. Small selected point card with orange accent rule.
Composition: one large straight-on mobile app viewport, 390x844 proportions, centered on a pale gray presentation canvas, no angled phone or hands, no device hardware. Small concept title above, a compact two-button interaction swatch below the viewport labeled "通常" and "ホバー", showing normal orange #E87524 and darker burnt orange #B84A12. Both buttons have white text "+ 場所を追加". Make the UI readable and plausible, not a marketing poster. White #FAFAFA and gray #E5E7EB dominate, warm orange is the sole strong accent. Translucency should visibly reveal subtle map lines through panels; avoid excessive gloss, rainbow, blue, purple or illegible pale text.
Viewport content: minimal header with "pin note", Material Design-style undo left arrow, redo right arrow, account outline icon, all spaced to fit smartphone. Grayscale Japanese urban map with several orange map pins, small location button. Compact bottom call-to-action "この場所を追加". Bottom navigation two items "地図" and "ポイント", active map highlighted orange. No oversized hero slogan, no login form, no extra features. Japanese sans-serif typography. Maintain 44px touch targets. Show a tasteful semi-transparent surface treatment consistent across components. This is design exploration, not a screenshot of a running app.
